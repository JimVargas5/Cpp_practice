#include <iostream>
using namespace std;

int main(){
    cout << "poop" << endl;
    return 0;
}

// use the following commands in git bash:

// >>> g++ study0.cpp -o study0.exe
// >>> ./study0.cpp

// the first line compiles the program and specifies the output, named study0.exe'
// the output with default to 'a.exe'
// use the './' to run the executable output

// alternatively, compile and run in one line with
// >>> g++ study0.cpp -o study0.exe; ./study0.exe